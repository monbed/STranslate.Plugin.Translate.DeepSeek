## 新增

- 翻译请求遇瞬时故障（连接失败、超时、限流 429、服务端 5xx）时按 MaxRetries / RetryDelayMilliseconds 配置自动重试（此前配置项定义但未生效）；密钥错误等确定性失败不重试，用户取消不受影响
- 流式返回无文本输出时给出明确错误提示（多语言）

## 修复

- 流中 API 错误事件（限流、配额不足等）此前被静默忽略、表现为空译文，现在正确报错
- 流式响应仅清理行首的 data: 前缀，避免误删 JSON 字符串值中出现的 "data:"
- choices 为空数组的流数据行不再触发内部异常
- 流式数据行解析失败时记录调试日志，便于排查第三方服务兼容问题

## 变更

- 验证请求与实际翻译共用同一构建管线，验证时关闭思考模式并限制 max_tokens 不超过 128，降低验证消耗
- 请求固定为流式（解析仅支持流式）

## 移除

- 未生效的 Stream 配置字段
